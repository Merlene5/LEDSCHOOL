<!DOCTYPE html>
<html lang="en">
<!-- Basic -->

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <!-- Mobile Metas -->
    <meta name="viewport" content="width=device-width, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <!-- Site Metas -->
    <title>LED SCHOOL</title>
    <meta name="keywords" content="">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Site Icons -->
    <link rel="shortcut icon" href="#" type="image/x-icon" />
    <link rel="apple-touch-icon" href="#" />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="css/bootstrap.min.css" />
    <!-- Pogo Slider CSS -->
    <link rel="stylesheet" href="css/pogo-slider.min.css" />
    <!-- Site CSS -->
    <link rel="stylesheet" href="css/style.css" />
    <!-- Responsive CSS -->
    <link rel="stylesheet" href="css/responsive.css" />

    <!--presentation CSS-->
    <link rel="stylesheet" href="css/presentation.css">
    <!-- Font Awesome pour les icônes -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">


    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    
</head>

<body id="inner_page" data-spy="scroll" data-target="#navbar-wd" data-offset="98" class="bg-light">
<!--header-->
<?php include 'header.php'?>
    <!-- End header -->
    <!--section-->
    <section class="inner_banner">
        <div class="Superpose"></div> 
        <div class="container">
            <div class="full animate__animated animate__fadeInDown">
                <h3>Presentation</h3>
            </div>
        </div>
    </section>
    <!--endsection-->
    <!--section-->

    <div class="container my-5">
        <div class="row">
            <!-- Left Side Section (Title) -->
            <div class="col-md-3 border-right text-center animate__animated animate__fadeInLeft">
                <h2 class="font-weight-bold text">LED School</h2>
                <p style="text-align:justify">Une école dédiée aux métiers de l'entrepreneuriat, du digital et du management, formant les talents de demain avec une pédagogie pratique et immersive.</p>
            </div>
    
            <!-- Main Content Section (5 Columns) -->
            <div class="col-md-9">
                <div class="row">
                    <!-- Quoi Column -->
                    <div class="col-md-4 mb-4 animate__animated animate__fadeInUp">
                        <h3 class="text">Quoi ?</h3>
                        <p>LED School est une école innovante dédiée aux métiers de l’entrepreneuriat, du digital et du management. Nous préparons nos étudiants aux défis actuels du marché du travail, en leur offrant une formation pratique et immersive.</p>
                        <p>Notre approche pédagogique est axée sur des projets concrets et le développement de compétences directement applicables, alliant savoirs, savoir-faire et savoir-être.</p>
                    </div>
                    
                    <!-- Pourquoi Column -->
                    <div class="col-md-4 mb-4 animate__animated animate__fadeInUp">
                        <h3 class="text">Pourquoi ?</h3>
                        <p>Dans un monde en constante évolution, les compétences doivent s’adapter aux exigences du marché. Les métiers traditionnels se transforment et de nouveaux rôles émergent.</p>
                        <p>Les étudiants de LED School apprennent à anticiper ces changements, à innover et à saisir les opportunités, développant une mentalité adaptable pour un environnement professionnel dynamique et compétitif.</p>
                    </div>
    
                    <!-- Qui Column -->
                    <div class="col-md-4 mb-4 animate__animated animate__fadeInUp">
                        <h3 class="text">Qui ?</h3>
                        <p>À LED School, nos étudiants sont uniques. Ils sont sélectionnés pour leur potentiel, leur motivation et leur désir de transformer leurs aspirations.</p>
                        <p>Qu'ils soient jeunes déscolarisés, en reconversion ou diplômés sans emploi, ils partagent un objectif commun : acquérir des compétences pratiques, développer leur leadership et s'ouvrir à de nouvelles opportunités professionnelles.</p>
                    </div>
    
                    <!-- Où Column -->
                    <div class="col-md-6 mb-4 animate__animated animate__fadeInUp">
                        <h3 class="text">Où ?</h3>
                        <p>Située au cœur d'un écosystème d'innovation, LED School offre un cadre propice à l'apprentissage et à l'épanouissement. Nos étudiants interagissent avec des professionnels en activité et bénéficient d'un réseau d'entreprises partenaires pour des stages et des opportunités d'emploi.</p>
                        <p>Ce lieu de rencontre favorise l'échange d'idées et la collaboration, permettant à chacun de s'enrichir et de bâtir son avenir professionnel.</p>
                    </div>
    
                    <!-- Comment Column -->
                    <div class="col-md-6 mb-4 animate__animated animate__fadeInUp">
                        <h3 class="text">Comment ?</h3>
                        <p>À LED School, nous adoptons une approche pédagogique innovante qui place l’apprenant au centre de son parcours. Nos étudiants s’engagent dans des projets concrets et bénéficient de stages et d’ateliers en collaboration avec des professionnels en activité.</p>
                        <p>Nous offrons également un accompagnement personnalisé, soutenu par un mentor pour aider chaque étudiant à définir ses objectifs. Notre réseau d’entreprises partenaires facilite l’accès à des opportunités professionnelles.</p>
                        <p>Enfin, un environnement collaboratif valorise l'échange d'idées et le travail en équipe, permettant à chacun de s’enrichir mutuellement et de construire un avenir professionnel prometteur.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!--endsection-->

    <!-- Start Footer -->
    <?php include 'footer.php'?>
    <!-- End Footer -->

    <div class="footer_bottom">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <p class="crp">© Copyrights 2024</p>
                </div>
            </div>
        </div>
    </div>

    <a href="#" id="scroll-to-top" class="hvr-radial-out"><i class="fa fa-angle-up"></i></a>

    <!-- ALL JS FILES -->
    <script src="js/jquery.min.js"></script>
    <script src="js/popper.min.js"></script>
    <script src="js/bootstrap.min.js"></script>
    <!-- ALL PLUGINS -->
    <script src="js/jquery.magnific-popup.min.js"></script>
    <script src="js/jquery.pogo-slider.min.js"></script>
    <script src="js/slider-index.js"></script>
    <script src="js/smoothscroll.js"></script>
    <script src="js/form-validator.min.js"></script>
    <script src="js/contact-form-script.js"></script>
    <script src="js/isotope.min.js"></script>
    <script src="js/images-loded.min.js"></script>
    <script src="js/custom.js"></script>

    <script>
        /* counter js */

        (function ($) {
            $.fn.countTo = function (options) {
                options = options || {};

                return $(this).each(function () {
                    // set options for current element
                    var settings = $.extend({}, $.fn.countTo.defaults, {
                        from: $(this).data('from'),
                        to: $(this).data('to'),
                        speed: $(this).data('speed'),
                        refreshInterval: $(this).data('refresh-interval'),
                        decimals: $(this).data('decimals')
                    }, options);

                    // how many times to update the value, and how much to increment the value on each update
                    var loops = Math.ceil(settings.speed / settings.refreshInterval),
                        increment = (settings.to - settings.from) / loops;

                    // references & variables that will change with each update
                    var self = this,
                        $self = $(this),
                        loopCount = 0,
                        value = settings.from,
                        data = $self.data('countTo') || {};

                    $self.data('countTo', data);

                    // if an existing interval can be found, clear it first
                    if (data.interval) {
                        clearInterval(data.interval);
                    }
                    data.interval = setInterval(updateTimer, settings.refreshInterval);

                    // initialize the element with the starting value
                    render(value);

                    function updateTimer() {
                        value += increment;
                        loopCount++;

                        render(value);

                        if (typeof (settings.onUpdate) == 'function') {
                            settings.onUpdate.call(self, value);
                        }

                        if (loopCount >= loops) {
                            // remove the interval
                            $self.removeData('countTo');
                            clearInterval(data.interval);
                            value = settings.to;

                            if (typeof (settings.onComplete) == 'function') {
                                settings.onComplete.call(self, value);
                            }
                        }
                    }

                    function render(value) {
                        var formattedValue = settings.formatter.call(self, value, settings);
                        $self.html(formattedValue);
                    }
                });
            };

            $.fn.countTo.defaults = {
                from: 0,               // the number the element should start at
                to: 0,                 // the number the element should end at
                speed: 1000,           // how long it should take to count between the target numbers
                refreshInterval: 100,  // how often the element should be updated
                decimals: 0,           // the number of decimal places to show
                formatter: formatter,  // handler for formatting the value before rendering
                onUpdate: null,        // callback method for every time the element is updated
                onComplete: null       // callback method for when the element finishes updating
            };

            function formatter(value, settings) {
                return value.toFixed(settings.decimals);
            }
        }(jQuery));

        jQuery(function ($) {
            // custom formatting example
            $('.count-number').data('countToOptions', {
                formatter: function (value, options) {
                    return value.toFixed(options.decimals).replace(/\B(?=(?:\d{3})+(?!\d))/g, ',');
                }
            });

            // start all the timers
            $('.timer').each(count);

            function count(options) {
                var $this = $(this);
                options = $.extend({}, options || {}, $this.data('countToOptions') || {});
                $this.countTo(options);
            }
        });
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>

</body>

</html>